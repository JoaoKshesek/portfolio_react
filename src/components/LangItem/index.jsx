import { Container } from "./styles";
import { Paragraph } from "../Paragraph";
export function LangItem({ children }) {
    return (
        <Container>
            {children}
        </Container>
    );
}